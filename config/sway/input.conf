# GENERATED by bin/null-input -- do not edit by hand.
#
# The layout here is ONE OF THREE places it is set. The console reads
# /etc/vconsole.conf and the greeter reads localectl; null-input sets all
# three together and verify/check-keyboard.sh asserts they still agree.

# null-input layout gb
# null-input delay 600
# null-input rate 25
input type:keyboard {
    xkb_layout gb
    repeat_delay 600
    repeat_rate 25
}

# null-input tap enabled
# null-input natural disabled
# null-input speed 0
input type:touchpad {
    tap enabled
    natural_scroll disabled
    pointer_accel 0
}

input type:pointer {
    natural_scroll disabled
    pointer_accel 0
}
