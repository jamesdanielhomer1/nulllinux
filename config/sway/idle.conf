# GENERATED by bin/null-idle -- do not edit by hand.
# The ordering dim < screensaver < lock < screenoff < suspend is an
# INVARIANT (§8.5). Editing a stage here without dragging the later
# ones is how a lock comes never to fire.

# null-stage dim 150
timeout 150 'brightnessctl -s set 10%' resume 'brightnessctl -r'
# null-stage screensaver 240
timeout 240 '/opt/nulllinux/bin/null-screensaver' resume '/opt/nulllinux/bin/null-screensaver stop'
# null-stage lock 300
timeout 300 '/opt/nulllinux/bin/null-screensaver stop; /opt/nulllinux/bin/null-lock'
# null-stage screenoff 330
timeout 330 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"'
# null-stage suspend 1800
timeout 1800 'systemctl suspend'

before-sleep '/opt/nulllinux/bin/null-screensaver stop; /opt/nulllinux/bin/null-lock'
